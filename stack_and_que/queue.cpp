#include<iostream>
using namespace std;
#include<queue>
#include<vector>
#include<algorithm>
int main(int argc, char const *argv[])
{
    // queue<int>que;
    // que.push(1);
    // que.push(54);
    // que.push(67);
    // que.push(56);
    // cout<<que.front()<<endl;
    // que.pop();
    // cout<<que.front()<<endl;
    vector<int>a = {1,2,3,4,5};
    reverse(a.begin(),a.end());
    
    return 0;
}
